/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class ofDragInfo
 *
 * This file has been generated by dub 2.1.~.
 */
#include "dub/dub.h"
#include "events/ofEvents.h"


/** Set attributes (key, value)
 * 
 */
static int ofDragInfo__set_(lua_State *L) {

  ofDragInfo *self = *((ofDragInfo **)dub_checksdata_n(L, 1, "ofDragInfo"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 2: {
      if (DUB_ASSERT_KEY(key, "files")) break;
      self->files = **((vector< string > **)dub_checksdata_n(L, 3, "vector< string >"));
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "position")) break;
      self->position = **((ofVec3f **)dub_checksdata_n(L, 3, "ofVec3f"));
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int ofDragInfo__get_(lua_State *L) {

  ofDragInfo *self = *((ofDragInfo **)dub_checksdata_n(L, 1, "ofDragInfo", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub_hash(key, 3);
  switch(key_h) {
    case 2: {
      if (DUB_ASSERT_KEY(key, "files")) break;
      dub_pushudata(L, &self->files, "vector< string >", false);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "position")) break;
      dub_pushudata(L, &self->position, "ofVec3f", false);
      return 1;
    }
  }
  return 0;
}

/** ofDragInfo()
 * 
 */
static int ofDragInfo_ofDragInfo(lua_State *L) {
  try {
    ofDragInfo *retval__ = new ofDragInfo();
    dub_pushudata(L, retval__, "ofDragInfo", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub_error(L);
}



// --=============================================== __tostring
static int ofDragInfo___tostring(lua_State *L) {
  ofDragInfo *self = *((ofDragInfo **)dub_checksdata_n(L, 1, "ofDragInfo"));
  lua_pushfstring(L, "ofDragInfo: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg ofDragInfo_member_methods[] = {
  { "__newindex"   , ofDragInfo__set_     },
  { "__index"      , ofDragInfo__get_     },
  { "new"          , ofDragInfo_ofDragInfo },
  { "__tostring"   , ofDragInfo___tostring },
  { "deleted"      , dub_isDeleted        },
  { NULL, NULL},
};


extern "C" int luaopen_ofDragInfo(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "ofDragInfo");
  // <mt>

  // register member methods
  luaL_register(L, NULL, ofDragInfo_member_methods);
  // save meta-table in _G
  dub_register(L, "_G", "ofDragInfo", "ofDragInfo");
  // <mt>
  lua_pop(L, 1);
  return 0;
}
